apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: go-demo
spec:
  hosts:
    - "*"
  gateways:
    - istio-system/istio-ingress
  http:
    - route:
        - destination:
            host: go-demo
            subset: stable
          weight: 80
        - destination:
            host: go-demo
            subset: canary
          weight: 20
